#! user-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: user
    namespace: deploy
    labels:
        com.docker.compose.project: deploy
        com.docker.compose.service: user
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: deploy
            com.docker.compose.service: user
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: deploy
                com.docker.compose.service: user
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: user
                  image: deploy-user
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: DATABASE_URL
                      valueFrom:
                        secretKeyRef:
                          name: nextplay-secrets
                          key: database_url
                    - name: JWT_SECRET
                      valueFrom:
                        secretKeyRef:
                          name: nextplay-secrets
                          key: jwt_secret
                    - name: PASSWORD_PEPPER
                      valueFrom:
                        secretKeyRef:
                          name: nextplay-secrets
                          key: password_pepper
                    - name: PORT
                      value: "8083"
                  livenessProbe:
                    exec:
                        command: ['curl', '-f', 'http://localhost:8083/health']
                    initialDelaySeconds: 20
                    periodSeconds: 5
                    timeoutSeconds: 3
                    failureThreshold: 5
                  ports:
                    - name: user-8083
                      containerPort: 8083
